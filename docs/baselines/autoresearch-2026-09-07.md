# Autoresearch source analysis for the numerical 0.1 decision

Inspected 2026-09-07 using the official GitHub tree API and pinned README/program at revision 228791fb499afffb54b46200aca536f79142f117. Raw retrieval evidence is retained in evidence/sources; no upstream code was executed or reused.

Observed baseline: train.py is the editable training module; prepare.py provides fixed preparation/evaluation; program.md describes baseline establishment, one change, five-minute training, lower validation bits per byte, and keep/discard/crash accounting. The training budget excludes startup/compilation. This is a compact instructional loop, not evidence of enforced malicious-code containment or protected confirmation. The program's indefinite iteration and Git resets are upstream behaviors, not instructions to this task.

The pinned root tree contains .gitignore, .python-version, README.md, analysis.ipynb, prepare.py, program.md, progress.png, pyproject.toml, train.py and uv.lock. README declares MIT. No standalone root LICENSE appears in that exact tree. This refresh preserves the unresolved redistribution/attribution gate rather than inferring code-reuse rights. Numerical 0.1 independently implements a mathematical objective and two transparent policies; it distributes none of this upstream implementation.

The later comparable target remains a lawful pinned tiny training study, same hardware and objective, fixed five-minute trial windows with setup/agent costs separately included, complete failures and independently rerun finalists. That target needs exact dependencies/data rights, suitable approved hardware, allocated compute and protected evaluator evidence. A Rosenbrock comparison cannot establish superiority to autoresearch or complete the historical training/agent-search tasks.

Methodological references: [Pineau et al. (2021)](https://www.jmlr.org/papers/v22/20-303.html) motivates explicit reproducibility artifacts and reporting; it does not validate this study. [ACM artifact terminology](https://www.acm.org/publications/policies/artifact-review-and-badging-current) separates availability, artifact evaluation and independent result reproduction; its current page returned HTTP 403 in this audit, so no fresh wording is asserted. [W3C PROV-DM](https://www.w3.org/TR/prov-dm/) supplies provenance concepts; scientific support and contradiction remain project-defined.

Fresh official master still resolves to the same pinned revision. Source inspection confirms `prepare.py` lines 30–32 declare context length 2048, training budget 300 seconds and evaluation token count 40×524288; its evaluation function begins at line 344. `train.py` imports that boundary at line 26 and calls evaluation at line 613. The exact upstream metadata declares torch 2.9.1 from CUDA 12.8 plus eight other packages with lower bounds. These are observed upstream requirements, not dependencies adopted here.

Raw downloaded source text is ignored locally, not committed or distributed. The publication-safe retrieval manifest is [source-retrievals.json](../../evidence/source-retrievals.json); it records immutable locators, dates and SHA-256 identities without bundling upstream implementation.
