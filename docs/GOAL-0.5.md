# Research Continuum 0.5 working goal

Historical preparation contract. The exact 0.5.0 packet was subsequently approved,
published and anonymously verified; see [release evidence](../evidence/public-release-0.5.json).
The native-control section below records the observed preparation-time limitation.

Owner instruction: September 8, 2026. Continue development through version 0.5.
Owner-selected scope: local research workbench.

Deliver a complete local research workbench that lets a researcher freeze and
execute multiple numerical objectives and baselines, organize finite multi-study
campaigns, inspect ablations and uncertainty without overstated claims, reproduce
portable evidence in fresh executions, and explore every study, trial, failure,
cost and conclusion through an interactive offline evidence browser.

Completion requires implemented and verified end-to-end workflows, meaningful
negative tests and real packaged execution, usable first-run/recovery/reproduction
documentation, explicit environment limits, and a concrete versioned 0.5 artifact.
Public distribution of the new artifact requires approval of the concrete release
packet. Preserve all original 0.1 publication and independent human/qualified-review
gates, source identities and acceptance history; do not mark them completed by
implementing 0.5. Keep the 228-task long-term programme intact. Training, generated
agent experiments, hostile-code sandboxing, remote workers and broad scientific
superiority are not the selected 0.5 scope.

Use GPT-6 Astra. Work only within this project or its explicitly isolated worktree.
Do not add dependencies, spending, credentials or shared-host changes without
existing authorization. Existing approved 0.1 assets remain immutable.

## Delivery sequence and acceptance

| Milestone | Deliverable | Required verification |
| --- | --- | --- |
| 0.2 | Versioned registry of Rosenbrock, sphere and anisotropic quadratic objectives; random and grid baselines; coordinate refinement and fixed-step ablation | Independent known-answer controls, equal allowances, deterministic replay, invalid-protocol rejection, unchanged schema-1 constraints |
| 0.3 | Frozen multi-study campaign manifest, finite aggregate reserve, pause/resume, full inventory | No late study replacement or omission, cost retention, duplicate execution prevention, process interruption recovery |
| 0.4 | Paired uncertainty, explicit deterministic-sample limits, controlled ablation contrasts, portable campaign export and fresh reproduction | Missing/failed/unequal-cost cases stay inconclusive; interval arithmetic independently checked; tampering and cached-run relabeling rejected; runtime deviations retained |
| 0.5 | Interactive offline evidence browser, complete documentation, candidate archive | Keyboard/filter/detail navigation, narrow viewport and accessibility checks; archive-only complete workflow; release and human gates reported separately |

These are delivery slices mapped to later numerical, ablation, reporting,
reproduction and workbench outcomes. They do not replace or silently complete the
broader historical contracts in plan/tasks.json.

## Native Goal control

During 0.5 preparation, the native Goal recorded the incomplete 0.1 objective. The available
Goal API exposes creation and terminal status updates, but no objective editing.
The authorized Mac helper attempted the supported route. It confirmed that the
tool catalog has no objective editor and Computer Use refuses access to the Codex
app. Native Goal editing therefore requires the owner’s supported UI control; no
objective or accounting was changed. This file records the owner-approved working
goal, and the task title reflects 0.5, without claiming a native Goal edit.
